#include "main.h"

int main(void)
{
	char *filename = "pwd";
	char *pathname = "/usr/bin";
	char *abs;

	abs = cat_file_path(filename, pathname);
	if (abs)
	{
		printf("%s\n", abs);
		free(abs);
	}
	else
		perror("Unable to get path.\n");
	return (0);
}
